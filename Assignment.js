const kelvin="293"
//Const is what will be used for this
const celsius = kelvin - 273
//What happened here is that we made a variable called celsius and to get its value we did kelvin=293 - 273=20
fahrenheit = celsius * (9/5) + 32
let fahrenheit = celsius * (9 / 5) + 32;
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);


 